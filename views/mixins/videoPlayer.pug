mixin videoPlayer(video= {})
    .videoPlayer#jsVideoPlayer
        video(src=`/${video.src}`)
        .videoPlayer__controls
            .videoPlayer__column
                input.videoPlayer__volume#jsVolume(type="range", min="0", max="1", step="0.1", value="0.1")
                .icon
                    span#jsVolumeBtn   🔊
                    span
                        span#currentTime 00:00:00
                        |/
                        span#totalTime 00:00:00
            .videoPlayer__column
                .icon
                    span#jsPlayButton    ▶
            .videoPlayer__column
                .icon
                    span#jsFullScreen    🔳